# @a1int スコープは GitHub Packages から取得（共通UIパッケージ @a1int/ui）。
# 認証トークンは環境変数 NODE_AUTH_TOKEN（read:packages 権限のPAT）。
# Amplify のビルド環境変数に NODE_AUTH_TOKEN を設定すること（ローカル開発も同様）。
@a1int:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
