/**
 * プログレスバー（円形スピナー）。全社共通の実体は @a1int/ui に集約。
 * 既存の import パス（'../components/ProgressBar'）は互換のため維持する。
 * ※ 勤怠は従来どおり既定のアクセント色（#4285F4）で表示（accentColor 未指定）。
 */
export { ProgressBar } from '@a1int/ui';
