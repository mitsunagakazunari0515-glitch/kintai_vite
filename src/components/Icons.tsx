/**
 * 汎用アイコン（全社共通・単一ソース）。実体は @a1int/ui に集約。
 * 形状・既定サイズ・既定色は勤怠を正典として一致させているため見た目は不変。
 * 既存の import パス（'../components/Icons' 等）は互換のため維持する。
 */
export {
  EditIcon, DeleteIcon, PlusIcon, RefreshIcon, SaveIcon, CancelIcon,
  UploadIcon, SearchIcon, NewFileIcon, PdfIcon, CheckIcon, MenuIcon, CloseIcon,
  ChevronDownIcon, ChevronUpIcon, ViewIcon, WarningIcon, InfoIcon, type IconProps,
} from '@a1int/ui';
